export { CatalogProvider, useCatalog, withCatalog } from "./catalog-provider";
export {
  filterProducts,
  formatCount,
  normalizeCategoryId,
  resolveInitialCategoryId,
  sortOptions,
  type FiltersState,
  type SearchParams,
  type SortValue,
} from "./catalog-state";
