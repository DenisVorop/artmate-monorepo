import type {
  AdminUserDTO,
  AdminUserStatus,
  UserRole,
} from "@/shared/actions/users";

export type AdminUser = AdminUserDTO;

export type AdminUserRole = UserRole;

export type UserAccountStatus = AdminUserStatus;
