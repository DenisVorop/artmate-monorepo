# Artmate Admin

Отдельное Next.js-приложение для административной панели Artmate.

```bash
yarn workspace admin dev
yarn workspace admin check-types
yarn workspace admin lint
yarn workspace admin build
```

Порт разработки по умолчанию: `3003`.
